# Package Information
[build-system]
requires = ["setuptools >= 77.0.3"]
build-backend = "setuptools.build_meta"


[project]
name = "ASHES"
version = "1.6"


[project.scripts]
ashes = "ashes_fg.cli:main"
