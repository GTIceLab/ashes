# Package Information
[build-system]
requires = ["setuptools >= 77.0.3"]
build-backend = "setuptools.build_meta"


[project]
name = "ASHES"
version = "1.6"
dependencies = [
  "numpy",
  "shapely",
  "verilog-parser",
  "lxml"
]

[project.scripts]
ashes = "ashes_fg.cli:main"
